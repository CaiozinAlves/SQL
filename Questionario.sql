-- 1) seleçao de banco do servidor 
-- user databese;

-- 2) exibir as tabelas do banco de dados 
-- show tables; 

-- 3) detalhes de uma tabela 
-- describe table;

-- 4) qual o significado do termo unsigned
-- a campo nao aceita valores negativos

-- 5) key? pri ? 
-- os dois termos indicam chave primaria respectivamente.

-- 6) qual o objetivo do comando select ?
-- exibir resultados de pesquisa em uma tabela.

-- 7)  * (asterisco) serve para exibir todas as colunas  de uma tabela 
-- devemos evitar para nao comprometer a perfomace do banco 

-- 8) o que significa WHERE?
-- e um condicionamento que ultilizamos para filtrar uma pesquisa , sim e  similar ao IF uma linguagem  de programaçao de alto nivel 

-- 9) BETWEEN ?
-- para restringir um intervalo de busca .

-- 10) quais os operadores que podemos ultiliza?
--  varios > , < ,  !, =,or , and , etc.....alter

-- 11) order  by
-- ordenar pesquisas de forma  decrecente e crescente.

-- 12) Count = conta o numero de linhas
-- 13) Sum = soma os valores de atributos  passado como paramentro 
-- 14) Max = seleciona o maior valor do atributo passado como parametro
-- 15) Min =  seleciona o menor valor do atributo passado como parametro 
-- 16) Avs = faz a media dos valores de um campo
-- 17) Distinct = evitar duplicadas e um campo